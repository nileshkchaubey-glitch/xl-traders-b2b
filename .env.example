# ====== XL Traders — copy to .env, fill, and NEVER commit .env ======
SUPABASE_URL=https://danoeaftaazhbldeeuxj.supabase.co

# SECRET (service_role) key — full admin access. Local only.
# ⚠️ Regenerate this in Supabase (Settings > API) before using, since the old one was shared.
SUPABASE_SECRET_KEY=paste-your-FRESH-secret-key-here

SUPABASE_BUCKET=product-images

# ---- OPTIONAL: AI image generation (only if you want `gen-image`) ----
# Works with any OpenAI-images-compatible endpoint. Leave blank to disable.
# IMAGE_GEN_URL=https://api.openai.com/v1/images/generations
# IMAGE_GEN_KEY=your-image-api-key
# IMAGE_GEN_MODEL=gpt-image-1
# IMAGE_GEN_SIZE=1024x1024
